apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8s-ingestor.fullname" . }}
  labels:
    {{- include "k8s-ingestor.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "k8s-ingestor.selectorLabels" . | nindent 4 }}
